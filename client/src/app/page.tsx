import HeroPanel from "@/components/panels/HeroPanel";
import MenuPanel from "@/components/panels/MenuPanel";

const Page = () => {
    return (
        <>
            <HeroPanel />
            <MenuPanel />
        </>
    );
};

export default Page;
